from setuptools import setup

setup(
    name='minimal',
    version='0.1',
    description='the most minimal package possible',
    author='brakkum',
    license='MIT',
    packages=['minimal'],
    zip_safe=False,
)
