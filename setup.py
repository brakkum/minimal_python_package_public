from setuptools import setup

setup(
    name='minimalpublic',
    version='0.1',
    description='the most minimal package possible',
    author='brakkum',
    license='MIT',
    packages=['minimalpublic'],
    zip_safe=False,
)
